The first column in the training and test sets represents the sum of read counts of the cluster after clustering, while the second column represents the read count of the individual sequence itself. During training, these two values are transformed using a specific formula and used as labels for model training.

